# Support triage assistant

An OpenAI Agents SDK assistant that looks up a support ticket and drafts a
reply for the support engineer. Tools live under `agents/` as
`@function_tool` functions and the assistant is wired in `agents/root.py`.
