name: gate
on: [pull_request]
jobs:
  policy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fleet capability policy
        continue-on-error: true
        run: ./scripts/check-capability-policy.sh
