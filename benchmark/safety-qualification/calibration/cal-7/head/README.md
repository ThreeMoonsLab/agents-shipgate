# fleet-ops assistant

Assistant over the fleet service. The tool surface comes from the deployment's
capability profile (`/etc/fleet/capability-profile.yaml`, or `$FLEET_PROFILE`),
which the platform team provisions per region.
