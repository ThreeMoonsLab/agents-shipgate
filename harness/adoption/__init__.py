"""Automated cold-agent adoption harness for agents-shipgate.

Drives coding agents (Claude Code, Codex CLI, Cursor) across a matrix of
(archetype, variant, prompt) cells and scores their behavior against the
adoption rubric. Local-only; not packaged.
"""
