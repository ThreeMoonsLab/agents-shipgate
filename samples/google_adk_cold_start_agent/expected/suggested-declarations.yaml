# Declaration questionnaire generated by agents-shipgate.
#
# Declaration questions: 2 of 12 answered; 10 open (6 effect, 4 authority).
#
# Ordered by how much answering can move the verdict. First, the actions
# nothing has pinned down: no effect evidence at all, or only a reading this
# scan is not allowed to act on. An action nothing has bounded is unmeasured,
# not safe, and its answer can still turn out to be anything. Then the ones
# the scan did establish, strongest first: money, outward communication,
# destruction.
#
# Each block below is one blank this repository still owes before it
# can reach a `passed` verdict. Replace every <REVIEW_REQUIRED>
# with a reviewed value, merge the block into shipgate.yaml at the path
# named above it, then re-run verification. One block answers both of
# an action's questions where it has two — they are one manifest row —
# and a `tool_sources[].authority` block answers for every action that
# source contributes, since they run with one credential. An
# `action_surface.actions` row still overrides it for one action.
#
# Where a value is already filled in, the scan observed the evidence
# printed above it and proposes the most conservative reading of that
# evidence — it is never weaker than anything observed. Keep it to
# confirm it, or replace it with a value you can defend. Nothing here
# is operative until you merge it into shipgate.yaml yourself.
#
# Everything else is a human declaration on purpose. Agents Shipgate
# will not guess a tool's authority or which object is the root agent,
# and a block still containing <REVIEW_REQUIRED> closes nothing.
#
# Every blank carries the values it accepts, or the shape its answer
# takes, on the comment line above it. Where a field is only required
# for some answers the comment says so — delete the lines your answer
# does not take.

# ── Question 1 of 10 · authority · adk_ops [tool_source] ────────────────────
---
# closes: missing_authority_evidence
# merge into: shipgate.yaml#tool_sources[id='adk_ops'].authority
id: adk_ops
authority:
  # accepted: none | scoped | unscoped | ambient
  mode: <REVIEW_REQUIRED>
  # how the credential behind this authenticates, in your own words (api_key,
  # oauth2, service_account, workload_identity, …). Required for every mode
  # except `none`; delete this line for `none`.
  auth_type: <REVIEW_REQUIRED>
  # the exact permission strings this source's credential is granted, one per
  # line. Required and non-empty for `mode: scoped`; must be empty (delete
  # this block) for every other mode.
  scopes:
  - <REVIEW_REQUIRED>
  # why this authority is the right one. Required for `unscoped` and
  # `ambient`; optional otherwise.
  reason: <REVIEW_REQUIRED>

# ── Question 2 of 10 · effect · update_case_index [adk_ops] ─────────────────
# This scan read nothing about this action's effect — an absence of evidence,
# not evidence that it is safe. Your answer is the only thing that bounds it.
# It is asked before the ones the scan could read for itself.
---
# closes: missing_effect_evidence
# merge into: shipgate.yaml#action_surface.actions[tool='update_case_index']
tool: update_case_index
tool_id: tool_v2_db009292df34bcaef93453d02b11fabfe531b942b738acabf52518924c290551
source_id: adk_ops
# accepted: read | write | destructive | external_communication |
# financial_write | production_operation | privileged_data_access |
# code_execution | identity_access
effect: <REVIEW_REQUIRED>
# which evidence this answer was given against, derived by the scan — keep it
# as written. It records what was readable here, not that anyone read it; when
# the evidence above changes, this question comes back instead of the answer
# quietly outliving it.
basis: confirmed:4f53cda18c2b

# ── Question 3 of 10 · effect · assemble_case_timeline [adk_ops] ────────────
# This scan read nothing about this action's effect — an absence of evidence,
# not evidence that it is safe. Your answer is the only thing that bounds it.
# It is asked before the ones the scan could read for itself.
---
# closes: missing_effect_evidence
# merge into: shipgate.yaml#action_surface.actions[tool='assemble_case_timeline']
tool: assemble_case_timeline
tool_id: tool_v2_43db0c35a05652febc69729ced4597557c72fe9ac42d66e927072c0d86bb15d9
source_id: adk_ops
# accepted: read | write | destructive | external_communication |
# financial_write | production_operation | privileged_data_access |
# code_execution | identity_access
effect: <REVIEW_REQUIRED>
# which evidence this answer was given against, derived by the scan — keep it
# as written. It records what was readable here, not that anyone read it; when
# the evidence above changes, this question comes back instead of the answer
# quietly outliving it.
basis: confirmed:4f53cda18c2b

# ── Question 4 of 10 · effect · ops.export_case_bundle [adk_ops:mcp:1] ──────
# Assumed in the absence of evidence, and never proposed from:
#   write — mcp_protocol_default  (heuristic only)
---
# closes: missing_effect_evidence
# merge into: shipgate.yaml#action_surface.actions[tool='ops.export_case_bundle']
tool: ops.export_case_bundle
tool_id: tool_v2_fb77b610591b1e630849b8b4c0cd838e57a26ec58d6ab17a86365b5b1656abdd
source_id: adk_ops:mcp:1
# accepted: read | write | destructive | external_communication |
# financial_write | production_operation | privileged_data_access |
# code_execution | identity_access
effect: <REVIEW_REQUIRED>
# which evidence this answer was given against, derived by the scan — keep it
# as written. It records what was readable here, not that anyone read it; when
# the evidence above changes, this question comes back instead of the answer
# quietly outliving it.
basis: confirmed:4f53cda18c2b

# ── Question 5 of 10 · authority · ops.export_case_bundle [adk_ops:mcp:1] ───
# declared authority conflicts with source authority evidence
#
# No block is offered for this one: Correct the source declaration or add a
# conservative reviewed action declaration, then rerun verification. Target:
# shipgate.yaml#action_surface.actions[tool='ops.export_case_bundle'].

# ── Questions 6–7 of 10 · effect, authority · ops.queue_backfill [adk_ops:… ─
# Assumed in the absence of evidence, and never proposed from:
#   write — mcp_protocol_default  (heuristic only)
---
# closes: missing_effect_evidence, missing_authority_evidence
# merge into: shipgate.yaml#action_surface.actions[tool='ops.queue_backfill']
tool: ops.queue_backfill
tool_id: tool_v2_da4d18a595cde84bc21e3deed46a0a2b4957fa41137a05e21135d79c1915d5c3
source_id: adk_ops:mcp:1
# accepted: read | write | destructive | external_communication |
# financial_write | production_operation | privileged_data_access |
# code_execution | identity_access
effect: <REVIEW_REQUIRED>
# the exact permission strings this action is granted, one per line. Required
# and non-empty for `mode: scoped`; must be empty (delete this block) for
# every other mode.
scopes:
- <REVIEW_REQUIRED>
authority:
  # accepted: none | scoped | unscoped | ambient
  mode: <REVIEW_REQUIRED>
  # how the credential behind this authenticates, in your own words (api_key,
  # oauth2, service_account, workload_identity, …). Required for every mode
  # except `none`; delete this line for `none`.
  auth_type: <REVIEW_REQUIRED>
  # why this authority is the right one. Required for `unscoped` and
  # `ambient`; optional otherwise.
  reason: <REVIEW_REQUIRED>
# which evidence this answer was given against, derived by the scan — keep it
# as written. It records what was readable here, not that anyone read it; when
# the evidence above changes, this question comes back instead of the answer
# quietly outliving it.
basis: confirmed:4f53cda18c2b

# ── Question 8 of 10 · effect · list_case_attachments [adk_ops] ─────────────
# This scan read nothing about this action's effect — an absence of evidence,
# not evidence that it is safe. Your answer is the only thing that bounds it.
# It is asked before the ones the scan could read for itself.
---
# closes: missing_effect_evidence
# merge into: shipgate.yaml#action_surface.actions[tool='list_case_attachments']
tool: list_case_attachments
tool_id: tool_v2_c83a1d833bb328bb24722817825d4917a91d3926c465010a73a5f3d4f2c19e07
source_id: adk_ops
# accepted: read | write | destructive | external_communication |
# financial_write | production_operation | privileged_data_access |
# code_execution | identity_access
effect: <REVIEW_REQUIRED>
# which evidence this answer was given against, derived by the scan — keep it
# as written. It records what was readable here, not that anyone read it; when
# the evidence above changes, this question comes back instead of the answer
# quietly outliving it.
basis: confirmed:4f53cda18c2b

# ── Question 9 of 10 · effect · issue_goodwill_refund [adk_ops] ─────────────
# What this scan read this action's effect as:
#   financial_write — risk_hint:keyword  (heuristic only)
# Proposed below: financial_write — at or above every reading here, so
# confirming it can only over-declare. Replace it if you can defend a
# different reading.
---
# closes: inferred_effect_only
# merge into: shipgate.yaml#action_surface.actions[tool='issue_goodwill_refund']
tool: issue_goodwill_refund
tool_id: tool_v2_8771e84514b39d47ac2a8d80ebda6542f05a19b662815e22a2bfad395964c611
source_id: adk_ops
# proposed from the evidence above — keep it to confirm, or replace it.
# accepted: read | write | destructive | external_communication |
# financial_write | production_operation | privileged_data_access |
# code_execution | identity_access
effect: financial_write
# which evidence this answer was given against, derived by the scan — keep it
# as written. It records what was readable here, not that anyone read it; when
# the evidence above changes, this question comes back instead of the answer
# quietly outliving it.
basis: confirmed:8850c34c2510

# ── Question 10 of 10 · authority · support.get_update_history [adk_ops:mc… ─
---
# closes: missing_authority_evidence
# merge into: shipgate.yaml#action_surface.actions[tool='support.get_update_history']
tool: support.get_update_history
tool_id: tool_v2_5e1f00840542297f77e9fbe5a1443f7cff77846ad2260cfcd93cc3702c4896db
source_id: adk_ops:mcp:1
# the exact permission strings this action is granted, one per line. Required
# and non-empty for `mode: scoped`; must be empty (delete this block) for
# every other mode.
scopes:
- <REVIEW_REQUIRED>
authority:
  # accepted: none | scoped | unscoped | ambient
  mode: <REVIEW_REQUIRED>
  # how the credential behind this authenticates, in your own words (api_key,
  # oauth2, service_account, workload_identity, …). Required for every mode
  # except `none`; delete this line for `none`.
  auth_type: <REVIEW_REQUIRED>
  # why this authority is the right one. Required for `unscoped` and
  # `ambient`; optional otherwise.
  reason: <REVIEW_REQUIRED>
