{
  "tools": [
    {
      "name": "lookup_case",
      "description": "Look up read-only metadata for an existing support case.",
      "inputSchema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {"case_id": {"type": "string"}},
        "required": ["case_id"]
      },
      "annotations": {"readOnlyHint": true},
      "auth": {"mode": "none"}
    },
    {
      "name": "summarize_case",
      "description": "Summarize read-only support case metadata for reviewer context.",
      "inputSchema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {"case_id": {"type": "string"}},
        "required": ["case_id"]
      },
      "annotations": {"readOnlyHint": true},
      "auth": {"mode": "none"}
    },
    {
      "name": "FileReadTool",
      "description": "Read a local file through the process filesystem authority.",
      "inputSchema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {"file_path": {"type": "string"}},
        "required": ["file_path"]
      },
      "annotations": {"readOnlyHint": true},
      "auth": {"mode": "ambient"}
    }
  ]
}
